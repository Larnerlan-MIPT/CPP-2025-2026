После ввода числа в буфере остается символ переноса строки (\n). Когда программа пытается читать строку, она сразу видит этот \n и останавливается.

Чтобы исправить, нужно перед чтением строки убрать этот лишний символ. Проще всего добавить пробел перед %[^\n]: scanf(" %[^\n]", str);